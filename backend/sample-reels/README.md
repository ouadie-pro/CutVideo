# Sample Reels

Drop any `.mp4` file into this folder. It will automatically
appear in the style reference picker on the frontend.

Each video is analyzed once (cached as `<filename>.profile.json`)
for its editing style (cut rate, motion, caption presence) so you
can generate new reels that match its rhythm.
